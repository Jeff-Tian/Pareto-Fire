import React from 'react'
import { Dimmer, Header, Modal } from 'semantic-ui-react'

export default class RefundPopup extends React.Component {
  render() {
    let { open, refundInfo } = this.props

    return <div>Helloer</div>

  }
}